# 小秧排表 - 剑网 3 团队管理系统

一个现代化的剑网 3 游戏副本团队管理系统,支持多群组管理、角色管理、开团排表、报名统计等功能。

## 项目概述

这是一个完全重构的新项目,旧代码位于 `old/` 目录仅供参考。

### 核心功能

- 🔐 **系统管理**: 管理员后台,群组权限管理
- 👥 **用户管理**: 用户注册、角色绑定、权限控制
- 🏰 **群组管理**: 多群组支持,群主自主管理
- ⚔️ **角色管理**: 游戏角色信息、心法、装分管理
- 📅 **开团系统**: 副本排表、模板管理、时间规划
- 📝 **报名系统**: 在线报名、状态跟踪、数据统计
- 📊 **数据统计**: 团队数据分析、出勤统计

## 技术栈

### 后端

- **框架**: FastAPI 0.109
- **数据库**: PostgreSQL 16
- **ORM**: SQLAlchemy 2.0 (异步)
- **认证**: JWT + bcrypt
- **数据验证**: Pydantic v2
- **文档**: OpenAPI (Swagger)

### 前端

- **框架**: React 18
- **构建**: Vite 7
- **UI**: HeroUI (NextUI v2) + Tailwind CSS 4
- **状态**: Zustand
- **请求**: SWR + Axios
- **路由**: React Router v6

## 快速开始

### 前置要求

- Node.js 18+
- Python 3.10+
- PostgreSQL 16+

### 后端启动

```bash
cd backend

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 配置数据库连接等

# 初始化数据库
psql -U postgres -f ../sql/schema.sql

# 创建管理员账号
python scripts/create_admin.py

# 启动服务
uvicorn app.main:app --host 0.0.0.0 --port 9500 --reload
```

访问 API 文档: http://localhost:9500/api/docs

### 前端启动

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

访问前端页面: http://localhost:5173

### 默认账号

- **用户名**: admin
- **密码**: 123456.

## 项目结构

```
yang-paibiao/
├── backend/                 # 后端项目
│   ├── app/
│   │   ├── api/            # API 路由
│   │   ├── core/           # 核心配置
│   │   ├── models/         # 数据模型
│   │   ├── schemas/        # Pydantic 模型
│   │   └── utils/          # 工具函数
│   ├── scripts/            # 脚本工具
│   └── tests/              # 测试
├── frontend/               # 前端项目
│   ├── src/
│   │   ├── api/           # API 接口
│   │   ├── components/    # 组件
│   │   ├── layouts/       # 布局
│   │   ├── pages/         # 页面
│   │   ├── stores/        # 状态管理
│   │   └── utils/         # 工具函数
│   └── public/            # 静态资源
├── sql/                    # 数据库脚本
├── docs/                   # 文档
└── old/                    # 旧项目(仅供参考)
```

## 开发进度

### ✅ 已完成

- [x] 数据库设计
- [x] 后端框架搭建
- [x] 管理员认证接口
- [x] 前端项目初始化
- [x] 登录功能实现
- [x] 路由守卫配置

### 🚧 进行中

- [ ] 群组管理接口
- [ ] 角色管理接口
- [ ] 前端群组管理页面

### 📋 待开发

- [ ] 开团系统
- [ ] 报名功能
- [ ] 数据统计
- [ ] Docker 部署
- [ ] 单元测试
- [ ] E2E 测试

## 开发文档

- [API 设计文档](./docs/api-design.md)
- [数据库设计](./sql/schema.sql)
- [后端 README](./backend/README.md)
- [前端 README](./frontend/README.md)
- [前后端联调测试](./frontend/TESTING.md)
- [AI 协作日志](./CHANGELOG-AI.md)

## 部署

### 开发环境

```bash
# 后端
cd backend && uvicorn app.main:app --reload

# 前端
cd frontend && npm run dev
```

### 生产环境

```bash
# 后端
cd backend && uvicorn app.main:app --host 0.0.0.0 --port 9500

# 前端构建
cd frontend && npm run build

# 使用 Nginx 部署静态文件
```

## 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

MIT License

## 联系方式

如有问题或建议,欢迎提 Issue。

---

**注意**: 旧项目代码位于 `old/` 目录,仅供业务逻辑参考,不再维护。
