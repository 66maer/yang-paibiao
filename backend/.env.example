# ============================================
# 小秧排表系统 - 环境配置模板
# ============================================
# 使用说明：
# - 开发环境：复制为 .env 并修改相关配置
# - 生产环境：复制为 .env.production 并修改相关配置
# - 注意：请勿将 .env 或 .env.production 提交到代码仓库

# ============================================
# 应用基础配置
# ============================================
APP_NAME=小秧排表系统
APP_VERSION=1.0.0

# 环境类型：development / production
ENVIRONMENT=development

# 调试模式（生产环境务必设置为 False）
DEBUG=True

# ============================================
# 服务器配置
# ============================================
HOST=0.0.0.0

# 端口配置
# - 开发环境：建议 9500
# - 生产环境：建议 8000
PORT=8000

# ============================================
# 数据库配置
# ============================================
# 数据库连接 URL
# - 开发环境示例：postgresql+asyncpg://yangpaibiao_user:password@localhost:5432/yangpaibiao
# - 生产环境示例：postgresql+asyncpg://yangpaibiao_user:password@shared-postgres:5432/yangpaibiao
# 注意：生产环境的 DATABASE_URL 和 SECRET_KEY 可以在 docker-compose.prod.yml 中通过环境变量覆盖
DATABASE_URL=postgresql+asyncpg://yangpaibiao_user:YOUR_PASSWORD@localhost:5432/yangpaibiao

# 数据库连接池配置
# - 开发环境：较小的连接池
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10
# - 生产环境：建议更大的连接池
# DATABASE_POOL_SIZE=10
# DATABASE_MAX_OVERFLOW=20

# ============================================
# JWT 认证配置
# ============================================
# JWT 密钥（生产环境务必修改！）
# 🔒 生成强密钥：openssl rand -base64 48
SECRET_KEY=CHANGE_THIS_TO_A_STRONG_SECRET_KEY_IN_PRODUCTION

# JWT 算法
ALGORITHM=HS256

# Token 过期时间
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# CORS 配置
# ============================================
# 允许的跨域来源
# - 开发环境：本地前端地址
CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173"]
# - 生产环境：您的域名
# CORS_ORIGINS=["https://zyhm.fun", "https://www.zyhm.fun"]

# ============================================
# 管理员账号配置
# ============================================
# 🔒 初始管理员账号（容器启动时自动创建，幂等操作）
# ⚠️  生产环境部署前务必修改为强密码！
# ⚠️  首次登录后立即修改密码！

ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
ADMIN_EMAIL=admin@example.com

# ============================================
# 日志配置（可选）
# ============================================
# 日志级别：DEBUG / INFO / WARNING / ERROR
# LOG_LEVEL=INFO
